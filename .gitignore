# .gitignore

# Dependencies
# This directory contains all the installed packages. It can be very large
# and should be reinstalled by running `npm install` on any new machine.
node_modules/

# Environment Variables
# This file contains your secret API keys and should NEVER be committed to GitHub.
# It is good practice to create a `.env.example` file to show what variables
# are needed, but the .env file itself must be ignored.
.env

# Generated Output
# This directory is created by the server to store the generated audio clips,
# temporary video files, and the final presentation.mp4. It should not be
# part of the source code.
public/

# Operating System Files
# These files are generated by various operating systems and are not part of the project.
.DS_Store
Thumbs.db
._*

# Log files
# Any log files generated during development or production.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE / Editor directories
# These directories are created by code editors to store project-specific settings.
.idea/
.vscode/
*.swp
*.swo